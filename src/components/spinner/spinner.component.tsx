import React from 'react';

import './spinner.styles.css';

import spinner from '../../assets/spinner.svg';

const Spinner = () => <img src={spinner} className="spinner" alt="spinner" />;

export default Spinner;
